extends Node

const card_text : Dictionary = {
	"Name" : "Elder Sea Monstrosity",
	"[ContactFusion]" : "2 cards with a void-triggered effect on the top of your deck, but void this card when it leaves the field. Only once per turn."
}

static func info():
	return card_text
